<div id="<%= dom_id task %>">
  <p>
    <strong>Titulo:</strong>
    <%= task.titulo %>
  </p>

  <p>
    <strong>Descricao:</strong>
    <%= task.descricao %>
  </p>

  <p>
    <strong>Vencimento:</strong>
    <%= task.vencimento %>
  </p>

  <p>
    <strong>Pronto:</strong>
    <%= task.done == true ? "Sim" : "Não" %>
  </p>

  <% if task.gif.present? %>
    <iframe src="<%= task.gif %>" width="380" height="280"></iframe>
  <% end %>

</div>


